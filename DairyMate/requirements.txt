# Dairy Mate console
streamlit>=1.36
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# udder model
opencv-python>=4.8.0
scikit-image>=0.22.0
scikit-learn>=1.3.0
joblib>=1.3.0
imbalanced-learn>=0.12.0

# sign-in (Google OIDC via st.login)
Authlib>=1.3.2

# teat model — heavy; skip it if you only need the udder pages
tensorflow>=2.16
# Keras comes in with TensorFlow — do not pin it here.
# The original teat_classifier.keras was saved by Keras 3.15.0, which needs
# Python >= 3.11. On Python 3.10 the newest Keras is 3.12.3, so use the
# rebuilt model instead (see README, "Python 3.10").
